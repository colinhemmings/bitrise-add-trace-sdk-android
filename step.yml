title: |-
  Add Trace SDK Android
summary: |
  Adds the Trace SDK to the given Android application.
description: |
  Adds the Trace SDK to the given Android application.
website: https://github.com/bitrise-steplib/bitrise-step-add-trace-sdk-android
source_code_url: https://github.com/bitrise-steplib/bitrise-step-add-trace-sdk-android
support_url: https://github.com/bitrise-steplib/bitrise-step-add-trace-sdk-android/issues
host_os_tags:
  - osx-10.10
  - ubuntu-16.04

project_type_tags:
   - android

type_tags:
  - utility

is_requires_admin_user: true
is_always_run: false
is_skippable: false
run_if: ""

deps:
  brew:
  - name: git
  - name: wget
  apt_get:
  - name: git
  - name: wget


toolkit:
  go:
    package_name: github.com/richard-bogdan-bitrise/bitrise-steplib/bitrise-step-add-trace-sdk-android


inputs:
  - example_step_input: Default Value - you can leave this empty if you want to
    opts:
      title: "Example Step Input"
      summary: Summary. No more than 2-3 sentences.
      description: |
        Description of this input.

        Can be Markdown formatted text.
      is_expand: true
      is_required: true
      value_options: []

outputs:
  - EXAMPLE_STEP_OUTPUT:
    opts:
      title: "Example Step Output"
      summary: Summary. No more than 2-3 sentences.
      description: |
        Description of this output.

        Can be Markdown formatted text.
